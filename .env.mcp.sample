# UI-TARS MCP Server Environment Configuration Sample
# Copy this file to .env.mcp and update with your actual values

# UI-TARS Agent Configuration
# Get your API key from Volcengine (火山引擎) console
AGENT_API_KEY=your_volcengine_api_key_here
AGENT_PROVIDER=volcengine
AGENT_MODEL=doubao-1-5-thinking-vision-pro-250428

# UI-TARS Command Configuration
# The base command for UI-TARS agent
TARS_COMMAND=agent-tars

# MCP Server Configuration
# WebSocket URL for MCP communication
UI_TARS_MCP_URL=ws://localhost:8080/mcp/ws

# Server host and port settings
UI_TARS_HOST=0.0.0.0
UI_TARS_PORT=8080

# Optional: Alternative providers and models
# Uncomment and modify if using different providers
# AGENT_PROVIDER=openai
# AGENT_MODEL=gpt-4-vision-preview
# AGENT_API_KEY=your_openai_api_key_here

# Email IMAP Configuration
# Configuration for IMAP email checking and retrieval
EMAIL_IMAP_SERVER=imap.gmail.com
EMAIL_IMAP_PORT_IMAP=993
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_app_password_here
EMAIL_TIMEOUT=30

# Email IMAP MCP Server Configuration
# WebSocket URL for MCP communication
EMAIL_IMAP_MCP_URL=ws://localhost:8081/mcp/ws

# Server host and port settings for the MCP server
EMAIL_IMAP_HOST=0.0.0.0
EMAIL_IMAP_PORT=8081

# Email IMAP Server Examples for different providers:
# Gmail: imap.gmail.com:993 (SSL)
# Outlook/Hotmail: outlook.office365.com:993 (SSL)
# Yahoo: imap.mail.yahoo.com:993 (SSL)
# iCloud: imap.mail.me.com:993 (SSL)

# Note: For Gmail, you need to use an App Password instead of your regular password
# Enable 2FA first, then generate an App Password at: https://myaccount.google.com/apppasswords